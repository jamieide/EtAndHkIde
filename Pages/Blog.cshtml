@page
@model EtAndHkIde.Pages.BlogModel
@{
    Layout = "Shared/_NavbarLayout";
    ViewData["Title"] = "Blog";
}

<div class="row">
    <div class="col-8 offset-2">
        <main>
            <h1 class="d-flex justify-content-between mb-5">
                <span>Blog</span>
                <i class="fas fa-bullhorn"></i>
            </h1>
            <div class="card">
                <div class="card-body">
                    <partial name="_BlogList" for="BlogEntries" />
                </div>
            </div>
        </main>
    </div>
</div>
@{
    Layout = "_BaseLayout";
}
@RenderSection("Head", false)

<partial name="_SiteNavbar" model="false" />

<div class="container">
    <div class="row">
        <div class="col-lg-3">
            <div id="sidebar">
                @RenderSection("Sidebar", false)
                <vc:page-metadata></vc:page-metadata>
            </div>
        </div>
        <div class="col-lg-9 order-first order-lg-last">
            <div id="body">
                @RenderBody()
            </div>
        </div>
    </div>
</div>
